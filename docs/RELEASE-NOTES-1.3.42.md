# Outsider 1.3.42

Private-beta evidence candidate superseding 1.3.41. Runtime PASSes from any
earlier product hash do not transfer to this artifact.

The optional DeepSeek Harness integration now separates three claims that were
previously collapsed. A durable correction acknowledgement establishes only
delivery. A new out-of-process deterministic matcher can establish a bounded
behavioral effect only when every pre-registered action reference occurs after
that acknowledgement and has a successful durable tool result. Semantic task
outcome, Outsider causal success, loss and liability remain false unless a
separate independent verifier supplies their own evidence.

The matcher binds the same pinned Harness session, correction,
`interventionId`, `correctionAuthorityHash`, durable acknowledgement event and
post-ack SessionEvent tool pairs. Unknown events, changed sessions, missing or
failed actions, duplicate expected references and any attempt by the in-process
plugin to declare outcome fail closed. The real Cordis waterfall canary now
proves delivery plus this bounded action effect; Stage 1 still ingests it as a
Way observation with `UNJUDGED` causal contribution.

The sibling Stage 1–4 research bridge now admits external exposure/loss data
only through two separately trusted Ed25519 signatures, keeps operator and
adjudicator identities on one side of temporal holdout splits, and requires a
third independently trusted signature over the exact frozen statistical packet
before model promotion or shadow clearing review. These research components
still emit no price, premium, coverage, capital, payout, capability or funds
movement.

The 1.3.41 deterministic R4 five-lane recovery experiment passed, but does not
transfer to 1.3.42. This candidate remains private beta until fresh immutable
R1–R5 evidence for this exact artifact, real Desktop Cowork conformance,
multi-hour endurance and an independent second-machine installation pass. Do
not hot-upgrade an active controlled session.
