# Outsider 1.3.41

Private-beta evidence candidate superseding 1.3.40. It does not inherit runtime
PASSes from any earlier product hash.

The typed correction-authority protocol now supports a hash-bound `delete`
action. A supervisor can request deletion only for a named path with the exact
preimage hash visible in controller evidence. The effect is credited only when
that exact file is absent after the worker acts. This closes the R5 failure in
which a valid request to remove the injected drift artifact was rejected as an
unsupported edit.

Every newly exported `outsider/supervised-experience/v2` record carries the
timestamp of the final sealed event as `source.observedAt`. Old records are not
backfilled from filesystem metadata; they remain explicitly unavailable for
temporal-forward evaluation.

The optional DeepSeek Harness reference integration includes an authenticated
out-of-process gateway, exact correction/ack hashes, a pinned runtime closure,
and a real Cordis waterfall canary using the official message factory. This is
durable delivery evidence only. Source-to-npm equivalence, a real work-session
effect, independent outcome verification, loss, liability, and clearing
authority remain unproven.

The Stage 1–4 research bridge separately ingests sealed Stage 0.5 Experience-v2
and the DeepSeek Way observation, keeps v1 compatibility rows out of all model
training, uses correlation-safe Claim × Way × World splits, and exposes a
research-only calibrated behavior stack plus dormant external frequency,
severity, and causal-capacity clearing layers. It cannot issue a price,
coverage, capital, payout, capability, or move funds.

This candidate remains private beta until fresh immutable R1–R5 evidence for
this exact artifact, real Desktop Cowork conformance, multi-hour endurance, and
an independent second-machine installation all pass. Do not hot-upgrade an
active controlled session.
