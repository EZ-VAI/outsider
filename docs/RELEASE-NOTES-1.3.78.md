# Outsider 1.3.78

1.3.78 fixes two deterministic failures exposed by the first immutable
1.3.77 two-hour R5 attempt.

- Exact controller-owned checkpoint shifts now recognize Claude Code's narrow
  `cd "<frozen workspace>" && <registered command>` host normalization. When
  the source fingerprint, prior independent PASS audit, action order, exits,
  and preregistered shift all match, the controller reuses that audited
  semantic outcome instead of buying two fresh model opinions every ten
  minutes. Other directories, commands, wrappers, and extra actions remain
  ineligible.
- The formal R5 supervisor ceiling is derived from the evidence obligations:
  four two-call patrols, the fixed team/recovery control path, and the existing
  eight-call completion reserve. This yields 28 logical supervisor calls, an
  expected no-retry path of 31 total processes, and a strict 61-process ceiling
  in the adversarial case where every JSON judge consumes its one permitted
  repair retry. The larger ceiling does not schedule extra calls; it makes the
  already-declared bounded retry protocol honest. Every evaluator-owned model
  process remains pinned to Sonnet/low.
- If the controller durably declares `run_cannot_recover`, the endurance
  evaluator now terminates the exact PTY process group and finalizes a
  conservative failed sample. It no longer waits indefinitely for an
  unreachable readiness predicate.

The interrupted 1.3.77 sample remains immutable negative evidence. All formal
R1-R5, Cowork, and independent-host evidence for a stable-public decision must
bind the 1.3.78 artifact and its exact source closure.
